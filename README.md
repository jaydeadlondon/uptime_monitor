# Uptime Monitor

Uptime Monitor — простой сервис для проверки доступности и времени отклика ваших сервисов и сайтов.

Ключевые характеристики

- Мониторинг HTTP/HTTPS-эндпоинтов
- Оповещения при недоступности (настройка через web-интерфейс)
- История проверок и базовые метрики доступности
- Разделённая кодовая база: бэкенд на Go, фронтенд на TypeScript (React + Vite)

Технологии

- TypeScript (фронтенд)
- Go (бэкенд)
- CSS

Установка и запуск (локально)

Требования:
- Go 1.20+ (для бэкенда)
- Node.js 18+ и npm/yarn (для фронтенда)

1) Клонировать репозиторий:

```bash
git clone https://github.com/jaydeadlondon/uptime_monitor.git
cd uptime_monitor
```

2) Запуск фронтенда:

```bash
cd frontend
npm install
npm run dev
# или
# yarn
# yarn dev
```

Фронтенд по умолчанию запускается в режиме разработки на http://localhost:5173 (Vite). Порт может отличаться — смотрите вывод команды dev.

3) Запуск бэкенда (пример):

```bash
# из корня репозитория
# если бэкенд расположен в подкаталоге, зайдите в него, например: cd server
go run ./...
```

Если в проекте есть файл .env.example, скопируйте его в .env и укажите необходимые переменные окружения перед запуском.

Конфигурация

- Переменные окружения и параметры проверок настраиваются через конфигурационные файлы или .env (в зависимости от реализации).
- Часто используемые переменные: адрес базы данных, ключи для отправки уведомлений, настройки интервалов проверок.

Использование

- Через веб-интерфейс: добавьте URL для мониторинга, настройте интервал и порог срабатывания.
- Смотрите историю проверок и уведомления в интерфейсе.

Разработка

- Следуйте общим практикам: запускайте линтеры и тесты перед созданием Pull Request.
- Для фронтенда используйте команды из папки `frontend` (npm run dev / build / lint).
- Для бэкенда используйте `go test ./...` для запуска тестов и `golangci-lint` (если настроен) для проверки стиля.

Вклад

PR и баг-репорты приветствуются. Описывайте шаги для воспроизведения и добавляйте минимальные тесты, если это возможно.

Лицензия

См. файл LICENSE в репозитории, если он присутствует. Если лицензии нет — добавьте подходящую (например, MIT) перед публичным использованем.

Контакт

По вопросам разработки и интеграции можете создать issue в репозитории.
